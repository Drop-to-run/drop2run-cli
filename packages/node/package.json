{
	"name": "@drop2run/node",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "Config, credentials and a filesystem source: the parts that need a machine.",
	"exports": {
		".": "./src/index.ts"
	},
	"files": [
		"src"
	],
	"engines": {
		"node": ">=20"
	},
	"scripts": {
		"typecheck": "tsc --noEmit",
		"test": "vitest run",
		"lint": "biome check .",
		"format": "biome check --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.11",
		"@types/node": "^26.4.0",
		"typescript": "^5.9.3",
		"vitest": "^4.1.11"
	}
}
